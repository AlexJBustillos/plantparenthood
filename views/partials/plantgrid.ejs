<a href="/plants/<%= item.id %>">
	<div class="plant-image">
		<% if (item.imageUrl){ %>
			<img src="<%= item.imageUrl %>" alt="<%= item.name %>">
		<% } else { %>
			<p class="no-image">Image Coming Soon</p>
			<% } %>
	</div>
	<h3 class="plant-grid-name"><%= item.name %></h3>
</a>
<h5 class="plant-grid-botanical"><em><%= item.botanicalName %></em></h5>
<span class="plant-icon-section">
	<% var lightMax = 4; %>
	<% for(var i = 1; i <= lightMax; i++) { %>
		<% if(item.light >= i) { %>
			<img src="/img/sun-full.png" class="plant-icon-small" alt="Light">
		<% } else { %>
			<img src="/img/sun-empty.png" class="plant-icon-small empty-icon" alt="Light">
		<% } %>
	<% } %>
</span>
<span class="plant-icon-section">
	<% var waterMax = 3; %>
	<% for(var i = 1; i <= waterMax; i++) { %>
		<% if(item.water >= i) { %>
			<img src="/img/water-full.png" class="plant-icon-small" alt="Water">
		<% } else { %>
			<img src="/img/water-empty.png" class="plant-icon-small empty-icon" alt="Water">
		<% } %>
	<% } %>
</span>